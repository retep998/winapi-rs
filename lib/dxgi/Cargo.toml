[package]
name = "dxgi-sys"
version = "0.2.0"
authors = ["Peter Atashian <retep998@gmail.com>"]
description = "Contains function definitions for the Windows API library dxgi. See winapi for types and constants."
documentation = "https://retep998.github.io/doc/winapi/dxgi/"
repository = "https://github.com/retep998/winapi-rs"
readme = "README.md"
keywords = ["windows", "ffi", "win32", "directx"]
license = "MIT"
build = "build.rs"
[lib]
name = "dxgi"
[dependencies]
winapi = { version = "0.2.5", path = "../.." }
[build-dependencies]
winapi-build = { version = "0.1.1", path = "../../build" }
